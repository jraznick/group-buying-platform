{
  "name": "group-buying-mvp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix api install && npm --prefix web install",
    "dev": "node -e \"console.log('Starting API on 4000 and Web on 3000\u2026')\" && (npm --prefix api run dev &) && (npm --prefix web run dev &) && wait"
  }
}